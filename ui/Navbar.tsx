import React from "react";

interface NavProps {}

const Navbar: React.FC<NavProps> = ({}) => {
  return (
    <nav>
      <ul>
        <li>A Link</li>
      </ul>
    </nav>
  );
};

export default Navbar;
